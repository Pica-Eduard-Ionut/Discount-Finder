services:
    discount-finder:
        build:
            context: .
            dockerfile: Containerfile

        container_name: recipehub

        ports:
            - "7001:5000"

        restart: unless-stopped
